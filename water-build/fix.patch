--- a/app/src/main/java/com/openai/ishrabmaapro/MainActivity.java
+++ b/app/src/main/java/com/openai/ishrabmaapro/MainActivity.java
@@ -365,6 +365,6 @@
     private static final class PathDrop {static void draw(Canvas c,Paint p,float x,float y,float size){android.graphics.Path path=new android.graphics.Path();path.moveTo(x,y-size);path.cubicTo(x-size*.25f,y-size*.55f,x-size*.72f,y,x-size*.72f,y+size*.35f);path.cubicTo(x-size*.72f,y+size*.95f,x-size*.3f,y+size*1.3f,x,y+size*1.3f);path.cubicTo(x+size*.3f,y+size*1.3f,x+size*.72f,y+size*.95f,x+size*.72f,y+size*.35f);path.cubicTo(x+size*.72f,y,x+size*.25f,y-size*.55f,x,y-size);c.drawPath(path,p);}}
     private final class BarChartView extends View {private final List<DataStore.DayTotal> values;private final int goal;private final Paint p=new Paint(Paint.ANTI_ALIAS_FLAG);BarChartView(List<DataStore.DayTotal> v,int g){super(MainActivity.this);values=v;goal=g;}@Override protected void onDraw(Canvas c){super.onDraw(c);float left=dp(12),right=getWidth()-dp(12),top=dp(18),bottom=getHeight()-dp(35);int max=goal;for(DataStore.DayTotal d:values)max=Math.max(max,d.total);float gap=dp(10),bar=(right-left-gap*(values.size()-1))/values.size();SimpleDateFormat f=new SimpleDateFormat("EE",ar?new Locale("ar"):Locale.ENGLISH);for(int i=0;i<values.size();i++){DataStore.DayTotal d=values.get(i);float ratio=max<=0?0:d.total/(float)max;float x=left+i*(bar+gap);float y=bottom-(bottom-top)*ratio;p.setColor(surface2);c.drawRoundRect(x,top,x+bar,bottom,dp(9),dp(9),p);p.setColor(d.total>=goal?0xFF00A88F:accent);c.drawRoundRect(x,y,x+bar,bottom,dp(9),dp(9),p);p.setTextAlign(Paint.Align.CENTER);p.setTextSize(dp(11)*textScale);p.setColor(muted);c.drawText(f.format(new Date(d.day)),x+bar/2,getHeight()-dp(12),p);}}}
 
-    private static abstract class PositionConsumer {abstract void accept(int position);}
+    @FunctionalInterface private interface PositionConsumer {void accept(int position);}
     private static final class SimpleItemListener implements android.widget.AdapterView.OnItemSelectedListener {private final PositionConsumer c;SimpleItemListener(PositionConsumer c){this.c=c;}@Override public void onItemSelected(android.widget.AdapterView<?> parent,View view,int position,long id){c.accept(position);}@Override public void onNothingSelected(android.widget.AdapterView<?> parent){}}
 }
